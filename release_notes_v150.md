# v1.5.0 新功能版本

## 🆕 新增功能

### #4 音标 + 系统 TTS 发音
- 识别/手动添加/AI 补全均支持 **IPA 音标**(如 /ˈʌnfetəd/),自动填充并入库
- **单词点一下朗读**:词列表小喇叭、词详情页点单词本体即读、生词本卡片/详情同样支持(系统 TTS,无需联网,基于设备语音引擎)
- 音标展示:词列表、词详情、生词本卡片、复习卡片全部显示

### #5 复习模式(我的 → 复习模式)
- 抽认卡:看词想义 → 翻面核对 → 标记掌握度(认识/模糊/不认识)
- 按掌握度筛选(全部/新词/学习中/已掌握)+ 打乱重来
- 标记实时写回生词本,本轮结束出统计小结

### ✍️ 写译批改(输入页 → 写译批改)
- 手写英文拍照/相册 → AI 转写 → 文本可编辑确认 → AI 批改
- 返回:分数 + 修正后全文 + 逐条点评(语法/拼写/用词/搭配/标点/自然度)+ 总体评语,可对照「我的原文」

### 🎨 其他
- 软件图标更换为新的 AI language 图标(全部 5 档分辨率)
- 数据库 v7(phonetic 字段迁移,旧数据自动升级)

## 安装
下载 app-release.apk 直接安装(arm64)。

## ⚠️ 注意
- 音标/朗读依赖 AI 返回与设备 TTS 引擎,无语音引擎时提示「设备未找到可用语音引擎」
- 识别结果现已包含音标字段,旧版本识别记录无音标(可在编辑中补充,或 AI 补全后保存)
