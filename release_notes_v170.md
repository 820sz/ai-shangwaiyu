# v1.7.0 四项实测问题修复

## 1️⃣ 思考强度「极致」修好了（根因）
- **根因**：档位校验只放行 `low/disabled`，DeepSeek 官方的 `high`/`max`（极致）被末尾兜底打回「不思考」——所以点了极致看起来像没生效。
- **修复**：档位合法性统一由**模型族档位表**判定（DS：不思考/低/高/极致；豆包：不思考/低），表内一律原样生效；表外旧值（豆包的 medium/high）仍迁移为低并写回，UI 与实际请求永远一致。
- 顺带修好：追问抽屉的思考档位同样支持「极致」；页头/设置页显示的档位不再与实际请求打架。

## 2️⃣ 识图的「重新识别」名副其实了
- 原来 AppBar 上那颗星只有**补充识别**（只并入漏掉的），没有真正重做识别 —— 现在拆成两个明确入口（识别质量菜单）：
  - **重新识别（校准）**：逐行扫描 → 找全标记位置 → 再读文字 → 输出前自检（逐条确认"图上确有标记痕迹"，不确定就删）。图片用**高清模式**（小字/浅色笔迹更可辨），结果**整组替换**（会先弹确认，避免误丢手动修改）。
  - **补充识别（只补漏）**：保留现有结果，只并入漏掉的。
- 首次识别的提示词也一起加强了：标记类型清单（圈画/下划线/双下划线/波浪线/荧光笔/方框/星号/箭头/书签贴/页边批注对应的英文）、"先扫标记再读字"、同处重复标记只输出一次。

## 3️⃣ 复习模式 5 个问题
1. **标记后不再跳过**：点「不认识/模糊/认识」→ 自动**翻面显示释义**（含例句、音标、保存时间）→ 看完点「下一张」。
2. **可前后翻卡**：卡片左右滑动（右滑上一张 / 左滑翻面或下一张）+ 底部 ⬅➡ 按钮，可回看上一张并显示当时标记结果。
3. **顶部对比度**：筛选 chip 与进度文字改深色加粗、进度条用主题色，不再看不清。
4. **分类 + 日期**：掌握度（全部/新词/学习中/已掌握）之外新增日期筛选（**今天 / 近3天 / 近一周 / 近一月**）；卡片显示「保存于 今天/昨天/N天前」；生词本卡片也显示保存时间（保存时自动记录）。
5. **进度会记住**：复习进度写入本地，退出再进会提示「上次复习到第 X 张」→「继续 / 重新开始」，不再每次从零随机重来。

## 4️⃣ 写译批改界面重做
- **长文不再撑破页面**：手写稿改成**固定高度横向缩略图条**（可左右滑、点击**全屏查看并双指缩放**），文本框改为**固定高度内部滚动**，页面始终能正常上下滑。
- **去掉了多余的灰色说明小字**：删掉"①…②…③…"流程说明、"批改返回:分数 + …"、"可一次选多张…"等一堆解释性文案。
- **全局瘦身**：输入页、我的、分类弹窗、写译记录等处的啰嗦副标题统一精简（功能名已经说清的，不再补灰字）。

## 验证
- `flutter analyze` 0 error / 0 warning
- `flutter test` **160 全绿**（+1 skip；新增 15 条：思考档位表外值/极致回归、复习筛选与进度持久化）
- arm64 release 构建，aapt：versionCode=47 / versionName=1.7.0
