# v1.6.0 写译批改重构 + 材料分组修复

## ✍️ 写译批改（整体重做，入口迁到「输出」页）
- **入口搬家**：从「输入」页移到「输出」页（写译属于输出），输出页顶部新增「写译批改」+「写译记录」两个入口
- **材料分类清晰**：进去先选 **手写档 / 电子档**
  - 手写档：可**一次上传多张**手写稿（拍照 / 相册多选，最多 9 张）→ 点「识别为电子档」合并转写 → 修改确认
  - 电子档：直接粘贴/输入英文
- **交互整理**：材料类型 → 上传 → 识别 → 确认 → 批改，步骤文案直接写在页面上；词数实时显示
- **批改结果**：分数 + 总体评语 + **错误分类汇总（词汇 / 语法 / 表达优化 / 其他）** + 逐条点评 + 修正后全文 + 我的原文
- **追问抽屉**：批改页右下角「追问」——和识图页**同一套**体验（收藏、选模型/槽位/思考档、上下楼记忆、编辑提问重发、历史对话），材料上下文自动带上原文+批改结果

## 📚 我的学习材料：书籍分组修复
- **一本书 = 一个文件夹**，页码/章节成为其下的子分类（两级展开）
- **旧数据自动迁移**（DB v8）：原来存成 `书籍/《X》/p16 p17` 的目录会归一为 `书籍/《X》` + 页码字段，同一本书不再按页分裂
- 页码排序按数字（p2 在 p10 前），未标页码归到「未标页码」子分类

## 🗂 写译练习日志
- 批改完成后弹「**是否保存此次写译练习**」，也可随时点「保存此次练习」
- 保存内容：原文、修正全文、分数、评语、逐条点评、四类错误汇总、手写原稿图、批改模型、时间
- 「输出 → 写译记录」按**日期文件夹**（如 2026年8月26日）归档，点开可看全部细节，可删除，用于复盘错误轨迹

## 🔧 内部
- 追问逻辑抽成公共 `FollowUpController`（识图页与写译页共用一份实现，不再各写一套）
- 数据库 v8：书籍路径归一 + `writing_logs` 表
- 新增 11 条回归测试（错误汇总解析、书籍分组、日志序列化）

## 验证
- `flutter analyze` 0 error / 0 warning
- `flutter test` 145 全绿（+1 skip）
- arm64 release 构建，aapt：versionCode=46 / versionName=1.6.0
