# v1.8.0 八项修复

## 1️⃣ 思考模式不再被砍成「不思考」（根因）
- **根因 A**：请求里写死 `max_tokens: 8192`，而它是 **思考+正文的总预算** → 思考一长就把正文吃光，正文为空（reasoning-only）。
  **修复**：DeepSeek 请求**不再发送 max_tokens**（与官方 / dsh 一致，交服务端默认预算）——思考档位越高越有空间。
- **根因 B**：检测到"只返回思考过程"就**自动把档位砍成不思考**。
  **修复**：改成先**从思考内容里把 JSON 抠出来用**（思考模型常把结果写在思考通道）；确实没结果时用**相同档位**重试一次；再失败就如实报错并附思考原文，**档位永远保持你选的值**。
- 追问抽屉同样处理：思考通道有答案就直接当回答显示。

## 2️⃣ 识图「重新识别」合并成一个动作
- 取消「校准 vs 补漏」的选择题 —— 现在只有一个 **重新识别**：逐行扫描 + 高清图 + 输出前自检，**既纠正错识也补齐漏识**，结果整组替换（手动补充的词会保留，弹窗里会写明条数）。

## 3️⃣ 复习模式
- **点「认识」直接进下一张**（不再强制翻释义）；只有「不认识 / 模糊」才翻面给你看释义，看完点「下一张」。
- **修掉「一个词反复点认识刷进度」的 bug**：同一张卡重复点同一档位不再计次；回看改标记会先撤销旧计数再加新计数 —— 统计数永远等于已标记卡片数。

## 4️⃣ 页码智能 + 书名可改
- 输入 `p9页` / `第9页` / `9` / `PP9页` → 统一存成 **p9**；`第9至12页` / `9~12` → **p9-12**；`p16 p17` → **p16-17**（连续自动压区间）；「序章」「前言」这类非数字原样保留。
- 旧数据自动归一（DB v9 迁移），不再有带 p / 不带 p 的杂乱。
- 「我的学习材料」里每个书/材料文件夹可**长按右侧铅笔重命名**，每个页码分组也可**直接改页码/章节**。

## 5️⃣ 「我的学习材料」数量对不上
- 根因：分类详情用默认 `limit=100` 读词，书籍 251 个只显示了 100 个。**已改为一次读全**，标题数量与实际一致。

## 6️⃣ 「其他输入材料」做成真正能用的功能
- **学习画像**（新增，可编辑、已保存）：英语水平 / 学习目的 / 偏好题材 / 补充说明；点「按词汇推断」会根据你的**真实词汇数据**（词汇量、来源书籍、分类分布）自动填建议，填过的内容不会被覆盖。
- **AI 推荐**：结合画像 + 你的生词数据 **流式**生成 4-6 份材料（含难度标签和"为什么推荐给你"的理由），**落库保存**，下次进来直接看，不再每次重算；想换一批点「重新推荐」。
- **点开就能学**：每条推荐可打开生成**精读内容**（📖选段 / 🇨🇳中文导读 / 🔑重点词与表达 / 🧭怎么用），同样是流式输出并缓存；可**保存为文章**进阅读器精读，可收藏，可**追问**。

## 7️⃣ 「AI 生成文章」迁到「输入」页
- 输入页新增 **特色功能 · AI 生词定制文章**（生成 + 文章列表 + 直接精读）；输出页不再重复，专注写译。

## 8️⃣ 识图结果页：选中模式下单击即选中
- 一旦进入选中模式（长按任一卡片），**单击 = 选中/取消选中**，不再弹"询问 AI 详解"；底部提示会变成「选中模式：单击 = 选中/取消 · 右上角可删除/保存」。

## 验证
- `flutter analyze` 0 error / 0 warning
- `flutter test` **176 全绿**（+1 skip；新增 16 条：页码归一、思考通道 JSON 提取、推荐解析、画像推断）
- arm64 release 构建，aapt：versionCode=48 / versionName=1.8.0
